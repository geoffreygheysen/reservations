<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <!--logo-->
        <a class="navbar-brand" href="#">Site Représentations</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <!--link-->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('show') }}">Spectacles</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('representation') }}">Représentations</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('artist') }}">Artistes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('category') }}">Catégories</a>
                </li>
                <!--btn inscription & connexion/deconnexion-->
                {% if not app.user %}                                    
                <li class="nav-right">
                    <form class="form-inline my-2 my-lg-0" action="{{ path('security_logout') }}">
                        <button class="btn btn-outline-success my-2 my-sm-0 mx-2" type="submit">Se connecter</button>
                    </form>
                </li>
                <li class="nav-right">
                    <form class="form-inline my-2 my-lg-0" action="{{ path('security_registration') }}">
                        <button class="btn btn-outline-primary my-2 my-sm-0" type="submit">S'inscrire</button>
                    </form>
                </li>
                {% else %}
                <li class="nav-right">
                    <form class="form-inline my-2 my-lg-0" action="{{ path('security_logout') }}">
                        <button class="btn btn-outline-danger my-2 my-sm-0 mx-2" type="submit">Se déconnecter</button>
                    </form>
                </li>
                {% endif %}

            </ul>
        </div>
    </nav>
</header>